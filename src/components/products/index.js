import React from "react";

const Products = ({ products }) => {
  console.log(products);
  return (
    <div class="h-screen bg-slate-300" id="products">
      Im Products container
    </div>
  );
};

export default Products;
